export default function splitString(inputString: string) {
  const characters: string[] = [];
  for (let i = 0; i < inputString.length; i++) {
    characters.push(inputString[i]);
  }
  return characters;
}
