import Footer from "@/features/front/components/sections/footer";
import type { FooterSectionProps } from "../shared/types";

/** Sección: Footer con créditos de JN Invitaciones. */
export default function FooterSection(props: FooterSectionProps) {
  return <Footer {...props} />;
}
